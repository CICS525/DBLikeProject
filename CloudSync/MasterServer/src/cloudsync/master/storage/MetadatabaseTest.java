package cloudsync.master.storage;

import static org.junit.Assert.*;

import org.junit.AfterClass;
import org.junit.BeforeClass;
import org.junit.Test;

import cloudsync.sharedInterface.Metadata;
import cloudsync.sharedInterface.Metadata.STATUS;

public class MetadatabaseTest {


    @Test
    public void testTestAddRecord() {
        //assertTrue(Metadatabase.testRecord());
    }
    
    @Test
    public void testAccept() {
        /*
        AccountDatabase.getInstance().createAccount("testuser", "testpass");
        
        Metadata meta = new Metadata();
        meta.filename = "file6";
        meta.parent = 0;
        meta.status = STATUS.LAST;
        
        Metadata m = Metadatabase.acceptFileUpdate("testuser", meta, "D:\\test.txt");
        System.out.println(m.status.toString());
        */
        
    }

}
